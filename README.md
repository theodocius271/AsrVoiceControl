# ASR Voice Control

## Description
--------------

This is an ASR voice control demo android app project. This project uses sherpa-onnx framework to deploy zipformer ASR model that 
predicts text result. The text result would be processed by a lightweight BERT embedding model to match pre-defined commands 
when ASR endpoint is reached.
